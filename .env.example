# Database (required)
DATABASE_URL=postgresql://notes_user:notes_password@localhost:5432/notesdb

# JWT Auth (required — use a long random string in production)
SECRET_KEY=changeme-generate-with-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=30
